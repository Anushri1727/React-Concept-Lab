const seoConcept = {
  id: "seo",
  title: "SEO in React",
  category: "SSR / CSR",

  definition:
    "Search Engine Optimization (SEO) is the practice of improving a website to increase its visibility in search engines. Standard React SPAs struggle with SEO because search engine crawlers often have difficulty or delays in indexing dynamically generated JavaScript content.",

  syntax: "// Best handled by SSR frameworks, or by using react-helmet in CSR",

  useCase:
    "If your application's success depends on people finding it via Google (e.g., e-commerce, blogs, marketing sites), you must address SEO. If it's a private application behind a login, SEO does not matter.",

  realLifeExamples: [
    "A product page on an e-commerce site needs accurate meta tags (title, description, image) so it looks good when shared on Twitter/Facebook and ranks well on Google.",
  ],

  codeExamples: [
    {
      id: "react-helmet",
      title: "1. Managing Head Tags (CSR)",
      description:
        "In a standard React app, you can use a library like 'react-helmet' to dynamically update the document <head> tags as the user navigates.",
      code: `import React from 'react';
import { Helmet } from 'react-helmet';

function BlogPost({ post }) {
  return (
    <div>
      {/* Helmet injects these tags into the document <head> */}
      <Helmet>
        <title>{post.title} - My Awesome Blog</title>
        <meta name="description" content={post.excerpt} />
        
        {/* Open Graph tags for social media sharing */}
        <meta property="og:title" content={post.title} />
        <meta property="og:image" content={post.imageUrl} />
      </Helmet>

      <article>
        <h1>{post.title}</h1>
        <p>{post.content}</p>
      </article>
    </div>
  );
}

export default BlogPost;`,
    }
  ],
  keyNotes: [
    "While Google's crawler CAN execute JavaScript, it often delays doing so to save resources. Other search engines and social media scrapers (Twitter, Facebook) often do not execute JS at all.",
    "For the best SEO, Server-Side Rendering (SSR) or Static Site Generation (SSG) via Next.js is highly recommended over Client-Side Rendering.",
    "Dynamic meta tags are crucial. Every distinct page/route in your app should have a unique <title> and <meta name='description'>.",
  ],
  commonMistakes: [
    {
      mistake: "Assuming standard React is SEO friendly",
      wrong: "Building a public marketing site using Create React App/Vite and expecting it to immediately rank well on Google.",
      correct: "Using Next.js (or Gatsby/Remix) for public-facing sites to ensure search engines receive fully populated HTML documents.",
    },
  ],

  interviewQuestions: [
    {
      question: "Why do standard React Single Page Applications (SPAs) struggle with SEO?",
      answer:
        "Because the initial HTML sent from the server is almost empty. The actual content is generated by JavaScript on the client side. Many search engine crawlers struggle to execute JavaScript reliably or quickly, leading to poor indexing of the site's content.",
    },
    {
      question: "How can you improve SEO in a React ecosystem?",
      answer:
        "The best solution is to use Server-Side Rendering (SSR) or Static Site Generation (SSG) using a framework like Next.js. For CSR apps, you can use libraries like 'react-helmet' to manage dynamic meta tags, though it's less reliable for web crawlers than SSR.",
    },
  ],
};

export default seoConcept;
